console.log("Hey there i am javascript");

// in node.js, there is only present core concepts of javascript nothing else , i mean javascript related to UI isn't here in node because they removed it so they can make it a backend language so all DOM, alert, window etc do not work in node 
// and many other changes like encryption, decryption, hashing are added